apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "express-hello.fullname" . }}-test-connection"
  labels:
    {{- include "express-hello.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "express-hello.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
