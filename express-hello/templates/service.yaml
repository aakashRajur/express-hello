apiVersion: v1
kind: Service
metadata:
  name: {{ include "express-hello.fullname" . }}
  labels:
    {{- include "express-hello.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.applicationPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "express-hello.selectorLabels" . | nindent 4 }}
